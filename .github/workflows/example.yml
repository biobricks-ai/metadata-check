name: Example - Validate BIOBRICK Metadata

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  validate:
    runs-on: ubuntu-latest
    name: Validate BIOBRICK.yaml
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Run BIOBRICK metadata validation
        uses: ./
        with:
          path: '.'

